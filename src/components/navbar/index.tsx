import ThemeToggle from '../theme-toggler';

const Navbar = () => {
  return (
    <div className='flex justify-between'>
      <p>Navbar</p>
      <ThemeToggle />
    </div>
  );
};

export default Navbar;
